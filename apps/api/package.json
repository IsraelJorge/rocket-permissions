{
  "name": "@repo/api",
  "version": "1.0.0",
  "description": "API for the repo",
  "scripts": {
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@repo/auth": "workspace:*"
  },
  "devDependencies": {
    "@repo/eslint": "workspace:*",
    "@repo/prettier": "workspace:*",
    "@repo/typescript": "workspace:*",
    "@types/node": "^20.14.9",
    "tsx": "^4.16.0"
  },
  "prettier": "@repo/prettier",
  "eslintConfig": {
    "extends": [
      "../../config/eslint/node.json"
    ]
  }
}
